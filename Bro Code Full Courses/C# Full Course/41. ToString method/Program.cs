// ToString() = converts an object to its string representation so that is suitable for display

using _41._ToString_method;

Car car = new Car("Chevy", "Corvette", 2022, "blue");

Console.WriteLine(car);

Console.ReadKey();